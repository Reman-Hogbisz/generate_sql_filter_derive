[package]
name = "generate_sql_filter_types"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"], optional = true }
serde_json = { version = "1.0", optional = true }
ts-rs = { version = "11.1", optional = true }

[features]
default = ["derive"]
derive = ["serde", "serde_json", "ts-rs"]
